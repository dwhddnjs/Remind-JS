// 삼항 연산자
// 기본 구조 condition ? true : false
const arr = [1, 2];
let text = arr.length === 0 ? "배열이 빔" : "배열이 비어있지 않음";

console.log(text);

const con1 = false;
const con2 = false;

const val = con1 ? "와우" : con2 ? "blabla" : "foo";

console.log(val);
